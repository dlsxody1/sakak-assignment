# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**주 사용자는 과제 평가자다.** 채용 과정에서 이 대시보드를 열어 지원자의 기술력과 설계
의도를 판단한다. 짧은 시간에 훑고, 화면이 코드에 대해 무엇을 말해주는지를 본다.

화면이 상정하는 **가상의 열람자**는 검진 결과를 확인하는 본인(비전문가)이다. 그 시나리오가
설득력 있어야 평가자가 설계 의도를 인정한다 — 두 층이 충돌하지 않는다.

## Product Purpose

건강검진 6회차 이력을 읽고 **지금 상태와 변화 방향**을 알려준다.

성공은 판정 로직의 정교함이 화면으로 증명되는 것이다. 특히 데이터가 3색으로 안 나뉜다는
사실(미측정·판정불가가 실재한다)을 회피하지 않고 드러내는 것이 이 제품의 핵심 주장이다.

## Positioning

대부분의 검진 대시보드는 정상/주의/위험 3색으로 뭉갠다. 이 제품은 **5-state**를 유지한다 —
*모르는 것*을 *괜찮은 것*으로 표시하지 않는다. 건강 데이터에서 이 구분은 정확성의 문제다.

## Operating Context

- 데이터 출처는 CANDiY API (`POST /v1/nhis/checkup`), 2단계 인증(토스, 4분 30초 제한)을 거친다
- 인증 없이도 화면을 볼 수 있어야 하므로 fixture를 같은 정규화 경로로 흘린다
- 평가자는 로컬에서 `pnpm dev` 로 열거나 배포된 링크를 본다

## Capabilities and Constraints

**확정된 것**
- 6회차(2020~2025), 검사항목 21개, 회차당 일반·구강 2건의 방문 이력
- 판정 5-state: `normalA` `normalB` `suspect` `unmeasured` `undetermined`
- 판정 로직은 `entities/health-reference`, 정규화는 `entities/checkup` — 구현 완료
- FSD 레이어와 세그먼트 규약을 경계 테스트가 강제한다 (`ui`에서 훅·fetch 금지,
  `shared/ui` 밖에서 도메인 콜백 props 금지)
- 차트는 ApexCharts

**데이터가 강제하는 제약**
- `height`·`weight`·`vision`·`hearing` 은 **기준 자체가 없다** (24건). 값이 구간을 벗어난
  진짜 판정불가(6건)와 성격이 다르므로 화면에서 구분한다 — 전자는 판정 배지 없이 수치만,
  후자는 판정불가 배지 + 사유
- `osteoporosis` 는 6회차 전부 결측
- 지질 4종은 2021·2023 결측 (격년 검사) → 차트에 구멍이 난다
- `waist` 는 질환의심 기준만 있어 정상 범위가 판정불가로 남는다
- **성별이 API 응답에 없다.** `waist`·`hemoglobin`·`yGPT` 판정에 필요해 로그인 폼에서 받는다

**미정**
- 로그인 Mock 방식, 라우트 경로 구조, 배포 방식

## Evidence on Hand

- `dashboard/src/shared/api/__fixtures__/checkup-response.json` — 실제 응답 구조를 유지한
  가상 인물(김건강) 데이터. 5-state가 전부 등장하도록 값을 의도 배치했다
- 판정 결과 실측: 126칸 = 정상 70 · 판정불가 30 · 미측정 14 · 질환의심 12
- 2025년이 가장 나쁘다 (질환의심 12건, API 종합판정도 `의심`)

**없는 것 — 지어내지 않는다**: 실제 환자 데이터, 의료진 검수, 사용자 조사, 성능 벤치마크.

## Product Principles

1. **모르는 것을 괜찮은 것으로 표시하지 않는다.** 미측정과 판정불가는 정상이 아니다.
2. **나쁜 쪽이 이긴다.** 기준이 겹치면 더 위험한 판정을 택한다 (혈압 OR 조건).
3. **판정 근거를 숨기지 않는다.** 왜 그 색인지 기준 문자열로 되짚을 수 있어야 한다.
4. **데이터가 없는 것과 로직이 실패한 것을 구분한다.** 화면이 고장난 것처럼 보이면 안 된다.

## Accessibility & Inclusion

- **색만으로 판정을 전달하지 않는다.** 5-state는 색 + 텍스트 라벨 + 형태로 중복 부호화한다.
  건강 데이터에서 적록색맹 사용자가 정상/위험을 구분 못 하는 것은 치명적이다.
- 명암비 WCAG AA 이상, 키보드 탐색 가능
